[package]
name = "sci-revm-shim"
description = "Compat shim that exposes revm 38's PrecompileOutput / PrecompileHalt / state-gas API surface on top of Base v0.9's revm 34. Consumed by sci-precompiles via Cargo `package = ...` rename so verbatim Tempo v1.7.1+ keychain source compiles without modification."

version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
revm.workspace = true
alloy-primitives.workspace = true
